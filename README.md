# SchoolFE

React + Vite frontend for the SchoolBE restaurant CRUD API.

## Run

1. Start the backend on port 5000.
2. Run `npm install`.
3. Copy `.env.example` to `.env` if the API uses another URL.
4. Run `npm run dev`.

Open the Vite URL shown in the terminal, normally `http://localhost:5173`.
